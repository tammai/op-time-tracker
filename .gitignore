node_modules/
out/
dist/
.vite/

# electron-builder output (the DMG/zip handed out for distribution). The icon
# sources under build/ are committed; the packaged artifacts are not.
release/
build/icon.iconset/

# Nuxt UI v4 (Vue-only) auto-generated type declarations
auto-imports.d.ts
components.d.ts

# OS junk
.DS_Store
Thumbs.db

# Editor dirs
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ESLint cache (written by the lint-fix-file.mjs PostToolUse hook)
.eslintcache

# Graphify: the graph itself is committed; cost ledger + AST cache are not
graphify-out/cost.json
graphify-out/cache/

# Disposable per-task spec (/task-workflow) — written on approval, deleted
# when the task ships. Never part of a commit.
PLAN.md
